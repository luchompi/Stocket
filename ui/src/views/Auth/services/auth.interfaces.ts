export interface userData {
    email: string,
    password: string,
    re_password?: string,
    username:string,
    first_name?: string,
    last_name?: string,
    phone?: string,
    address?: string,
    uid?: string
}